events { }
http {
    include mime.types;

    types {
      application/wasm;
    }

    server {
        listen 80;

        location / {
            root /usr/share/nginx/html;
            try_files $uri $uri/ /index.html =404;
            add_header Access-Control-Allow-Origin *;
        }
    }
}